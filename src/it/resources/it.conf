sqlserver = {
  profile = "slick.jdbc.SQLServerProfile$"
  db {
    driver = "com.microsoft.sqlserver.jdbc.SQLServerDriver"
    host = "192.168.64.11"
    port = 31433
    user = sa
    password = YOUR_PASSWORD_123_abcd

    url = "jdbc:sqlserver://"${sqlserver.db.host}":"${sqlserver.db.port}";user="${sqlserver.db.user}";password="${sqlserver.db.password}""
  }
}

prestodb = {
  db {
    host = "192.168.64.11"
    port = 32766
    catalog="hive"
    schema="default"
    user="hive"
    password="hive"

    url = "jdbc:presto://"${prestodb.db.host}":"${prestodb.db.port}"/"${prestodb.db.catalog}"/"${prestodb.db.schema}""
  }
}

spark {
  crd = "deployment/spark-prometheus.yaml"
}

kafka {
  host = "192.168.64.11"
  port = 9093
  url = ""${kafka.host}":"${kafka.port}""
  topics = ["my_msg_topic"]
}

hive {
  tableName = "default.test_1"
}



